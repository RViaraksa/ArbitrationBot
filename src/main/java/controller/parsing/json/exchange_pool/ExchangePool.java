package controller.parsing.json.exchange_pool;

import bean.Order;
import controller.parsing.json.bean.JsonObject;

import java.util.List;

public interface ExchangePool {
    static  List<Order> getListOrder(JsonObject object) {
        return null;
    }
}
